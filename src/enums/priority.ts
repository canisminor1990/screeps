export enum StructurePriority {
	spawn,
	extension,
	tower,
	extractor,
	link,
	container,
	terminal,
	lab,
	observer,
	powerSpawn,
	nuker,
	road,
	constructedWall,
	rampart,
}

export enum ManagerPriority {
	CreepManager = 1,
	RoomManager = 2,
}
